*.vs/
*bin/
*obj/
*out/
*Properties/
*.csproj
*.sln
*.ini
!mdk.ini
!*.mdk.ini
*.props
*.user
*.claude/

# the mod and the sim suite are built from these - see docs/development.md
!*.csproj
!*.sln
!*.props

# The blueprint corpus and the build output both live OUTSIDE this folder now - see
# Directory.Build.props and Blueprints.CorpusPath. This repository is the mod folder, linked into
# the game, so anything sitting in it is part of what gets published to the workshop. These entries
# remain so an older checkout that still has them does not commit them.
/corpus/
*.build/

# vstest blame dumps: multi-gigabyte, and this folder is published to the workshop
TestResults/

# Python bytecode, from the corpus scorers and their checks.
__pycache__/
*.pyc

# Jetbrains IDE
*.idea/